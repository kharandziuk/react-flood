on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Uses Node.js 12
        uses: actions/setup-node@v1
        with:
          node-version: 12
      - run: yarn -v
      - run: yarn
      - run: yarn build
